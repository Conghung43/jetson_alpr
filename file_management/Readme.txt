1. Connect to zed camera.
2. open config.py to check parameter, edit it yourshelf.
3. run the file_manenger.py, it will get the parameter from config.py then save the camera image and point_cloud to the saveing path.

**Create folder on every day.
**The saving path will be year_month_days and the saving file name will be hour_minutes_second_microsecond.